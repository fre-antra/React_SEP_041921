import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';
import reportWebVitals from './reportWebVitals';

ReactDOM.render(<App />, document.getElementById('root'));

// If you want to start measuring performance in your app, pass a function
// to log results (for example: reportWebVitals(console.log))
// or send to an analytics endpoint. Learn more: https://bit.ly/CRA-vitals
reportWebVitals();

// If you want to start measuring performance in your app, pass a function
// to log results (for example: reportWebVitals(console.log))
// or send to an analytics endpoint. Learn more: https://bit.ly/CRA-vitals


// notes
// npm: node package manager  help user install package
// npm server: npm install from npm registry
// npx: node package runner  temporary download after cra build will release those node file
// cra: use file system and generate something like script

// node-modules: npm install can reinstall it from package.json
// devdependencies: not relating to product only for development 
// dependencies: project need that

// ^5.12.0 semantic version system (above version 5)
// package-lock.json : npm tell which version you install installed    if you use code from repo, will use this one with npm i

// git: track file change
// npm run start ==  npm start (start test has their own shortcut)




















reportWebVitals();
