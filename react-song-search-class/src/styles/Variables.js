import { createGlobalStyle } from 'styled-components';

const Variables = createGlobalStyle`
  :root {
    --primary-color: teal;
    --secondary-color: #f0eeee;
    --navbar-height: 60px;
    --navbar-searchbar-width: 80%;
  }
`;

export default Variables;